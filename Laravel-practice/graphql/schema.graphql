type Query {
    users: [User!]! @paginate
    usered: [User!]! @all
    user(id: ID @eq): User @find
    posts: [Post!]! @all
    post(id: ID @eq): Post @find
}

#import post.graphql
#import user.graphql